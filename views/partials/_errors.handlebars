{{!-- Passport errors --}}
{{#if error}}
  <div class="col-md-6 mx-auto alert alert-danger">{{error}}</div>
{{/if}}

{{!-- Non-passport errors --}}
{{#each errors}}
  <div class="col-md-6 mx-auto alert alert-danger">{{text}}</div>
{{else}}
{{/each}}